# proxmox-bootc-vm module

Placeholder for a Proxmox-first bootc VM module contract.

Initial rules:

- No live provider configuration in the harness feature.
- Add variable validation blocks when variables are introduced.
- Keep HCP Terraform remote execution and state behavior documented before live plan/apply.
