# bootc_build: Ansible Role

This directory will contain the tasks and handlers for building a bootc image within the orchestration workflow.

- No active tasks in this scaffold.
- Upstream playbook: `playbooks/bootc_build.yaml`
