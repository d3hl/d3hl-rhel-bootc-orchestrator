# Architecture decisions

Use ADRs to lock decisions that affect ownership boundaries, live-action gates,
or contracts between image build, registry, HCP Terraform, and Ansible/AAP.

Start with `0000-adr-template.md`.

Accepted ADRs:

- `0001-ansible-first-bootc-flow.md` — Ansible-first orchestration and HCP Terraform boundary
