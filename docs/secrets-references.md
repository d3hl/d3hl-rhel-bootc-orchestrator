# Secrets references

Use the shared baseline in `agent-contract-master/docs/secrets-baseline.md`.

Placeholder references to define before live integration:

- HCP Terraform token: `op://d3HLPRV/<item>/TF_API_TOKEN`
- AAP controller credential: `op://d3HLPRV/<item>/<field>`
- Quay/private registry robot credential: `op://d3HLPRV/<item>/<field>`
- Red Hat registry/subscription credential: `op://d3HLPRV/<item>/<field>`
- Proxmox API credential: `op://d3HLPRV/<item>/<field>`

Do not replace placeholders with plaintext values.
