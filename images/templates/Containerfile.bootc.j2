ARG BOOTC_BASE_IMAGE=registry.redhat.io/rhel9/rhel-bootc:latest
FROM ${BOOTC_BASE_IMAGE}

# Initial template placeholder only. Future features must pin base image strategy,
# package inputs, validation, and registry promotion rules before building.
RUN true
